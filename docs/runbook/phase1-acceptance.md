# 全系统实施状态

> 文件名为历史兼容入口；当前状态已从 Phase 1 验收升级为 Phases 1–9 全部实现。

## 已完成范围
- [x] Phase 1：枚举、角色、主数据、客户、设置、审计、RLS。
- [x] Phase 2：采购订单、盲收、三方核对、价格历史与成本提醒。
- [x] Phase 3：批次、`qty_weight_lb` 双单位库存、单批拣货位、补货、盘点、ATP。
- [x] Phase 4：销售订单快照、信用①、毛利审批结构、冻结前闸门。
- [x] Phase 5：两步拣货、实重、差异枚举、生成拣货单锁单。
- [x] Phase 6：信用②、发运、签收锁定、释放预留、待开票队列。
- [x] Phase 7：退货提货、隔离、新批次、配送调整、贷项队列。
- [x] Phase 8：重包父子批次、投入产出与递归追溯。
- [x] Phase 9：统一单号、跨表校验、防环、视图权限及看板数据源。

## 数据库交付
- [x] 迁移 `0001`–`0017` 全部存在并按依赖顺序覆盖 Phases 1–9。
- [x] 增量迁移 `0018`–`0032`（IT 权限、原产品多包装、盲收强化、库存调整、称重收货、商品分类）已并入序列，明细见 [migrations.md](./migrations.md)。
- [x] 关键铁律由约束、触发器、函数、RLS 和 security-invoker 视图共同守护。
- [x] 六个业务视图：ATP、最高在库成本、待开票、信用占用、贷项队列、批次追溯。

## 文档交付
- [x] 十份模块文档均采用固定六节模板。
- [x] Schema 文档覆盖全部表、视图、主要触发器、RLS 与核心 ERD。
- [x] ADR 0001–0010 保留已采纳决策。
- [x] 迁移、部署与排障 runbook 可用。

## 部署前人工验证
1. 配置真实 Supabase 环境变量并连接目标项目。
2. 执行全部迁移与种子数据，确认 `0001`–`0032` 无跳号、无失败。
3. 为测试用户配置 admin、it、purchasing、warehouse、sales、sales_manager、account、finance、driver 角色。
4. 按角色验证盲收、锁单、两步称重、发运信用②、签收锁行、退货司机字段限制和重包追溯。
5. 对照外部财务系统确认信用 exposure 与待开票/贷项口径。

## 已知非阻断边界
- 完成表示数据库模型、护栏与文档已落地，不等同于所有生产集成和人工 UAT 已完成。
- FEFO 自动选批、漏称超时告警、`stock_mismatch` 自动建盘点任务仍需应用层编排。
- 本系统提供财务队列数据，不生成正式发票、收款或贷项通知单。
