# ADR-0003: 两步拣货（取货 / 称重分离）+ 周转筐绑定

## 状态
已采纳（Phase 5 实施）

## 背景
称重品占比高；取货与过秤若混在一步，现场排队严重且易漏称。

## 决策
① 取货扫筐/批号录件数 → ② 称重扫筐复核录 `actual_weight_lb`。筐是两步之间的载体。

## 后果
✅ 可并行；漏称可看板监控  
❌ 多一个状态 `picked_pending_weight`；该状态禁止装车（铁律 4）
