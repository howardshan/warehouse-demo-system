# ADR-0010: 财务不做，但财务数据现在必须存

## 状态
已采纳

## 背景
本期范围不开票、不收款核销；但信用与对账依赖发货金额与贷记。

## 决策
存 `invoice_status`、待开票/待贷记队列、SL `total_amount`；不做开票 UI。

## 后果
✅ 未来财务模块可接；信用现在就能算  
❌ 容易被误解为「已经开票」——文档与 UI 必须标明「待开票队列（只读）」
